import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 15,
  },
  description: {
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export default styles;
