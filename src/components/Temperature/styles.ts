import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  component: {
    alignItems: 'center',
  },
  text: {
    fontSize: 56,
    color: '#001858',
    fontWeight: 'bold',
  },
});

export default styles;
